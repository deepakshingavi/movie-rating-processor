package com.dp.training.model

/**
 *  Note : Class field naming intentionally avoid Java naming convention to match column
 *  name with Data fields
 */
case class MovieInfo(MovieID: Int, Title :String, Genres : Array[String])
